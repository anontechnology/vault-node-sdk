{
  "name": "javascript-client-2",
  "version": "0.1.0",
  "description": "A Javascript SDK for abstracting and simplifying transactions between the Anontech Nox REST API.",
  "license": "Unlicense",
  "main": "src/ViziVault.ts",
  "scripts": {
    "test": "mocha --recursive",
    "start:dev": "nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/ViziVault.js"
  },
  "browser": {
    "fs": false
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "date-fns": "^2.16.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "nodemon": "^2.0.6",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.5",
    "@types/mocha": "^8.2.0",
    "mocha": "^8.2.1"
  }
}
